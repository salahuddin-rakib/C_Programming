#include <stdio.h>
int main()
{
  int wide, height;
  printf("Enter the rectange's wide :");
  scanf("%d", &wide);
  printf("Enter the rectange's height :");
  scanf("%d", &height);
  printf("The area of rectangle is : %d", wide * height);
  return 0;
}