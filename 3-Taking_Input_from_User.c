#include <stdio.h>
int main()
{
  int a, b;
  printf("Give a number as input :");
  scanf("%d", &a);
  printf("Give another number as input :");
  scanf("%d", &b);
  printf("The addition of those two numbers is : %d", a + b);
  return 0;
}